// Hand-curated (not auto-generated): every currently-open real question
// this reverse-engineering project has, condensed from
// docs/reverse-engineering/events.md, rom-map.md, and this project's own
// task list. Each entry links back to the concrete real ROM
// addresses/mechanisms involved, not a vague "TODO".
const OPEN_QUESTIONS = [
  {
    title: "Cross-actor dispatch mechanism ($31AD / $C3F0 / $C3FE / $C3FF)",
    area: "Script interpreter",
    description: "A shared dispatcher with 15 independent real call sites resolves which script a given actor/context should run, reading a WRAM record at $C3F0/$C3FE/$C3FF. The MECHANISM is confirmed; the record's own general schema (per-room? per-actor? something else?) is not fully confirmed. UPDATE 2026-08-14 (task #86): a real, concrete structural clue found while tracing the boss-defeat block -- one real consumer ($24D4, a family of near-identical sibling blocks) dereferences a pointer stored at $C3FE/$C3FF, offsets it by 0-2 bytes depending on which sibling runs, then further indexes by the PLAYER'S OWN CURRENT FACING NIBBLE ($02AB) before calling $31AD. Reads like a pointer to a small per-context table indexed by [sibling block] + [player facing] -- a real lead, not yet the full schema. UPDATE 2026-08-16 (task #149, \"generalize the one-shot $31AD trigger into a re-armable one\"): $31AD's own internal real logic is now fully disassembled for the first time. It self-gates via bit 1 of real WRAM cell $C0A1 (RET NZ if already set) and re-sets that same bit (plus bit 2) on its own completion -- i.e. it is NOT hardware one-shot in the sense of 'fires at most once ever', it fires at most once PER busy period. The real ROM clears that gate itself, at the exact same moment opcode 0x00's own real handler ($3297) reaches its 'queue genuinely empty' halt (gated on a new, previously undocumented real WRAM cell, $D865 -- 0 means empty). A real live trace found a genuine SECOND $31AD firing (matching task #86's own $C5AF edge-transition timing almost exactly) that commits the EXACT SAME real entry point as the first invocation (bank 13, $470F) -- implemented and tested as BossSequenceInterpreter:rearm(), but WHY the real ROM reinvokes the identical entry a second time is honestly still open (a real deliberate repeat, or this project's own ctx callbacks simply not diverging where live different WRAM state would) -- not resolved this pass. UPDATE 2026-08-16 (\"$3213 weiterverfolgen\", whole-corpus-scan follow-up): the $24D4/$24F9/$251F sibling family's own real continuation ($3213 -> $31C7) is now DECISIVELY confirmed to be $31AD's own internal completion/dispatch tail itself -- same 3 special-case WRAM fallback buffers ($D613/$D623/$D633), same $3282/$3c4f/$3727 resolve-and-dispatch chain, same $C0A1/$C0A2 bit-1+2 re-arm. These are alternate ENTRY POINTS into the SAME mechanism (reached from the whole-corpus scan's own top-3 blocking opcode handlers, 132/1357 real scripts), not a separate system -- real, cross-corroborated confirmation the dispatcher has even more real call sites than the already-known 15. What remains open is exclusively DATA (which real actor/WRAM value each affected script resolves at runtime), not structure -- the same honest 'can't derive from a static shadow-run' category this project already accepts for opcodes 0x08-0x0C. MAJOR UPDATE 2026-08-19/20 (a full session dedicated to \"wie erreicht das Spiel Bank 14\"): the missing piece -- how $31AD's own resolved cursor picks bank 13 vs. the long-mysterious bank 14 -- is now FULLY reverse-engineered. $3282 (bank-0 fixed) resolves a 16-bit index (passed via HL, NOT BC -- a real, self-caught correction to this project's own earlier documented calling convention, found via a live injection that produced the WRONG table entry until fixed) against a real bank-8 lookup table (file 0x20F11, ~6263 usable entries -- also corrected: entries past index 1357 are NOT uniformly unprogrammed filler as this project's own scriptPointerTable doc previously claimed; real data continues to the table's true end). $3C4F then reads the resolved cursor's own high byte: <0x80 -> bank 13 (default); in [0x80,0xBF] -> bank 14 (with a -0x40 correction); >=0xC0 -> bank 13, unchanged. TWO independent, fully live-confirmed real triggers now exist for bank 14: (1) a forced-but-precisely-real CALL $31AD with the corrected HL=5744 (a table index whose value exactly matches a known CutTransitionTable landing record) drove $D86A/the real emulator bank state to 14 exactly as predicted; (2) a genuinely NATURAL one -- script opcode 0x02 (CHAIN) uses this exact same $3C4F correction (confirmed by fresh disassembly of $32FE), and one real script in the ORIGINAL, trustworthy 1357-script corpus (index 495) was found and live-verified to reach bank 14 this way, with zero forcing. A SEPARATE, independent real trigger was also found this session: script opcodes 0xC9/0xCA/0xCB (previously catalogued but with an untraced leaf) read a literal 16-bit index straight from script bytecode and spawn an independent parallel sub-script (WRAM $D613/$D623/$D633) via this same $3282 lookup -- script index 706 fires this unconditionally as its own first instruction, resolving to a real, recognizable CutTransitionTable-shaped record. HONEST, STILL-OPEN REMAINDER: what selects EITHER script 495 or 706 to run as a fresh top-level script in the first place is genuinely unknown -- every known real cross-script-reference mechanism this project has cracked (the CHAIN graph across the whole trusted corpus, the $26DC/roomSelectorTable grid, the WRAM queue at $D8BC/$D8BD, every literal-constant search) was checked and comes back negative for both. Both scripts remain real, live-confirmed, but genuine islands -- the natural game-state condition that starts them is this project's own next well-scoped puzzle, not a hypothetical."
  },
  {
    title: "Boss-defeat sequence: CLOSED -- it's an edge-triggered actor-cleanup detector, not a queue-gate halt",
    area: "Script interpreter",
    description: "CLOSED 2026-08-14 (task #86, live mGBA re-trace): the earlier 'queue-gate halt on the $C5A0 actor-command queue' explanation did NOT survive a direct live re-check ($D874 bit 0 never changes during the whole block; $C5A0 stays all-zero throughout) and was retracted. The REAL mechanism, fully live-verified: $1F35 selector 0x13 ticks every ~2500-3500 steps calling $4BE0, which recomputes a classified-actor count and reports 'ready' only on the SPECIFIC tick where that count (cached at $C5AF) transitions from nonzero to exactly 0 -- an edge detector, not a level check. Live-watched $C5AF directly: it sits at 0x01 for the whole ~200,000-step block, then flips to 0x00 right before the block releases. The real ~1.7s delay is the boss's own entity slot genuinely taking that long to finish despawning after its HP hits the dead sentinel -- real engine cleanup latency, not an arbitrary timer."
  },
  {
    title: "No real camera-scroll rendering (engine-wide, found via fourthRoom's corridor)",
    area: "Room system",
    description: "RESOLVED 2026-08-14 that this was never a coordinate bug: fourthRoom's real 'cut' exit (to fifthRoom) already uses the correct trigger coordinates (this project's player.x/y IS the same raw-WRAM coordinate space the ROM itself uses, no translation needed). The real, general gap found instead: Field.lua has no camera-scroll implementation at all, so a real ROM room that scrolls before its own 'cut' fires (like fourthRoom's own west corridor) never gets its scrolled-past content rendered here. That corridor was originally misread as a second real exit into a separate room ('sixthRoom') -- re-investigated under 'gamemap absolute prio' and retracted 2026-08-14, since it's really further columns of fourthRoom's own single continuous room space, not a genuine ROM cut. RE-WIRED 2026-08-15 anyway, as an explicit engineering choice (not a reversal of that finding): direct, repeated user confirmation that walking west should lead somewhere in this app, combined with sixthRoom's own real tile grid already sitting fully decoded and unused, outweighed strict ROM-transition fidelity -- the west exit is back, honestly labeled as a stand-in for real scroll-camera support rather than a claimed ROM fact. Also now hosts a second boss encounter (see the 'Second boss investigation' entry below)."
  },
  {
    title: "Collision: willyRoom closed, most other rooms still a visual heuristic, not a decoded ROM fact",
    area: "Room system",
    description: "CLOSED for willyRoom specifically (2026-08-14): its real metatile collision byte's own bit rule was re-derived from scratch (cross-tabulated against live-movement-tested ground truth, all 320 real grid cells, zero exceptions) -- collision==0x30 means floor in willyRoom's own table, the OPPOSITE polarity from fourthRoom/unknownRoomA's table (confirming collision semantics are set per metatile TABLE, not fixed ROM-wide). Wired in as willyRoom's real live collision, proven behavior-preserving both headlessly and via a live love . movement test. Still open: secondRoom/thirdRoom likely extend the SAME metatile table (2 real indices already found past willyRoom's own last entry) but aren't wired yet; fourthRoom/fifthRoom/sixthRoom/startRoom have no known metatile source of their own at all; unknownRoomA/unknownRoomB have real collision bytes but no live-movement ground truth is possible there (no gameplay ever reaches them), so their own rule stays unverified extrapolation. UPDATE 2026-08-15 (direct user demand: \"suche einen allgemeinen kollisions mechanismus\"): a dedicated hunt for fourthRoom's own real metatile+layout-stream source came back a genuine, decisive negative (single-stepped the ENTIRE real staircase transition, 6M+ instructions, zero hits on the known $242B RLE-decompressor entry -- this room's real load simply doesn't use that pipeline). Fell back to real, live-walked probing instead (mgba, held-button input + save/load_raw_state to reset between probes, teleporting the WRAM position directly was tried first and found unreliable in this environment) and found a real, position-specific wall the flat floorTileIds model structurally cannot express: the staircase landing is a narrow real alcove, not full-width floor, even though its own tiles are the identical IDs marked floor everywhere else in the room. Added a new, general, reusable escape hatch instead of another one-off: TileWalkability.build now accepts an optional room.blockedRects list (real, live-discovered rectangular exceptions layered on top of the tile-ID check), usable by any room. Caught and fixed a real off-by-one in the first version via a live love . screenshot before shipping. UPDATE 2026-08-19/20 (unknownRoomA's own floor data investigation): its collision polarity question is now CLOSED too, via visual verification rather than more byte-value statistics -- rendered all 6 real unknownRoomA rooms with a collision-value color overlay and looked directly at the art: collision==0x30 always renders as a real, recognizable brick/masonry wall texture, collision in {0x00,0x08} always renders as a real mesh/net floor texture (the SAME polarity fourthRoom already uses, not willyRoom's opposite one). Found a genuine DATA-MODEL limitation underneath, though: 4 of 82 real tile IDs conflict (real floor in most occurrences, wall-adjacent decoration in a few), which a flat tile-ID-keyed floorTileIds set cannot represent -- confirmed by trying it and watching connectivity collapse to 2-5 cells in 5 of 6 rooms. Resolved cleanly without a new data model: RoomExplorer.lua (the F8 dev-browser) already had a general, position-aware collision mechanism (TileWalkability.buildFromCollisionGrid, built for exactly this shape of data) -- it just had a real, unnoticed regression pointing bank-5 records 8-13 at the GENERIC catalog metatile table/tileset instead of unknownRoomA's own dedicated, verified pair. Fixed and live-verified via real screenshots matching this project's own independent render_unknown_room_a.py rendering exactly -- F8 now shows unknownRoomA with real art AND real, position-aware walkability. Connectivity into normal gameplay stays withdrawn regardless (still no live trigger into this room family, see the 'Room connectivity' entry below)."
  },
  {
    title: "The real cross-bank CHAIN mystery -- substantially resolved (task #81, 2026-08-16)",
    area: "Script interpreter",
    description: "7 real scripts CHAIN (opcode 0x02) to a cursor address that would fall in a DIFFERENT bank than the one currently mapped. UPDATE 2026-08-16 (task #81, direct user instruction \"erst 151 dann 81\"): a static+interpreted re-check found these were genuinely TWO different cases. Script 489 was never actually cross-bank at all -- its own operand fits in 16 bits, so the already-known $3c4f correction resolves it in-bank; re-shadow-running from there dispatches 41 distinct, richly varied real opcodes before hitting an ordinary, unrelated, already-known-hard opcode -- decisive confirmation this one was a false alarm (the 2026-08-13 tally predates the $3c4f fix). The other 6 (530/703/879/1141, plus a second CHAIN reached from 1324/1325) genuinely overflow the CPU's own 16-bit address space -- NEW, wired CANDIDATE hypothesis: reuse ScriptPointerTable.resolve's own already-proven 'roll into a later real bank' formula for CHAIN's operand bytes too. All 6 now resolve to valid in-window addresses with zero interpreter crashes, though the confirmation is weaker than script 489's case (some land in ambiguous long runs of opcode 0x00). Real code shipped: StandardScriptHandlers.chain() implements the full hybrid rule (onChainTarget gained a bankOffset argument), scripts/scan_all_scripts.lua now follows it. Corpus-scan impact: clean 875->884, error_other 320->310. Genuinely still open: no known live trigger reaches any of these 7 scriptPointerTable entries in normal gameplay, so the originally-planned live $2100-write watchpoint has its own unmet prerequisite -- the CANDIDATE (not VERIFIED) status for the 6 overflow cases stands until either a live trigger is found or further static evidence closes it."
  },
  {
    title: "Audio: DECODED and PORTED into love.audio -- a few real sub-questions remain",
    area: "Audio",
    description: "UPDATE: the 'completely unknown' status this entry originally described was resolved 2026-08-15 (real song table, note/duration/octave/frequency format, static disassembly of bank 15 -- see docs/reverse-engineering/audio.md) and PORTED into real, playing src/audio/ + love.audio code 2026-08-16 (task #151, MusicScore/GBSquareSynth/MusicPlayer + a dev-only MusicJukebox browser, F9 from Field.lua). Genuinely still open: exact musical intent of several driver commands (0xE2/0xE3/0xE7/0xE9/0xEB/0xEC) beyond their real WRAM side effect; the auxiliary vibrato/pitch-delta layer (0xE4) is disassembled but not played back; channel 3's own real hardware target is still unconfirmed (a live smoke test found one extreme, 65536Hz data point suggesting its bytes may not always mean 'pitch' the same way channels 1/2's do); no known real ROM trigger ties a specific song INDEX to a specific real game moment, so MusicJukebox stays dev-only."
  },
  {
    title: "Opcodes 0xA0/0xA1: real bank-1 targets never traced",
    area: "Script interpreter",
    description: "Both delegate through the already-mapped $1ED7 dispatcher (selectors 9 and 0x0A) to real bank-1 addresses $5136/$5156 -- confirmed live but genuinely new, un-disassembled territory. 0xA1 alone blocks 10 real scripts in the whole-corpus scan."
  },
  {
    title: "The palette-fade family's exact fade curve (0xBC / 0xBD)",
    area: "Script interpreter",
    description: "Both real handlers read two shared gradient lookup tables ($101A/$1030) indexed by the $D499/$D49A phase counter -- the mechanism is traced, but the tables' own real content hasn't been decoded into an exact per-frame fade curve, and 0xBD additionally calls a further untraced leaf ($1142)."
  },
  {
    title: "Task #150: reaching unvisited dialogue via WRAM injection -- blocked by pervasive bank call-stack churn, precisely understood but not solved",
    area: "Script interpreter",
    description: "The real digraph/low-byte gap (originally reported as '141 scripts failing StandardScriptHandlers.tick's byte classifier') mostly sits in dialogue this project has never reached via normal gameplay -- closing it the way text.md originally cracked the format (anchor on real, visible content) needs either organic gameplay progression to that dialogue (no known trigger) or forcing it via WRAM injection. Two 2026-08-15 passes found single-tick injection (poke currentBank+HL directly) gets overwritten within about 1 real frame. UPDATE 2026-08-16 (\"Scheduler-Frage weiterverfolgen\"): traced this precisely rather than just observing it. A refined injection (also overwriting the CURRENT top-of-stack slot in the real bank call-stack, $C000+$FF8A, so the interpreter's own $29FB/$2A0A round-trips restore the injected bank) works for ~1300 steps (~1 real frame) before reverting. A direct write watchpoint on $2100 itself (the real MBC2 bank-select register) proved EVERY bank switch traces to only $29FB/$2A0A -- no third mechanism -- but the SAME shared stack sees many distinct, unrelated real banks (1/2/3/13/15) pushed and popped within a ~3000-step window during a passive black-wipe cutscene alone. Decisive conclusion: the bank call-stack is a busy, general-purpose primitive shared by many legitimate, unrelated subsystems, so ANY one-off injected state at a specific stack depth is swept away by ordinary churn within about one real frame, regardless of which WRAM cells are targeted. Retracts the earlier, less precise 'two concurrent scripts' framing without fully overturning it -- refines WHY injection fails from an observation into a mechanism. UPDATE 2026-08-16 (4th pass, direct continuation): before touching live tracing again, instrumented the whole-corpus scan to record each failure's real ROM file offset, not just its byte value/script index -- decisive scope reduction: the '154 script-hits across 30 byte values' collapse to only 63 GENUINELY DISTINCT real failure locations (e.g. byte 0x05, the single largest blocker at '20 scripts', is really only 3 distinct real locations -- scriptPointerTable indices 38/39/40 resolve to CPU addresses one byte apart, the same overlapping-table-entry phenomenon task #81 already found for CHAIN targets). One word-solve attempt on the top real location decoded as '----d[0x05]' (real HYPHEN_BYTE x4 + already-known digraph 0x30='d') -- inconclusive, reported honestly rather than guessed at. Real prerequisite for closing the remaining 63 locations: either a genuinely surgical, sustained per-instruction 'puppeteering' driver (re-injecting on every relevant dispatch, not once), or reaching the content organically. Neither attempted further -- correctly time-boxed a 4th time, but with a much smaller, more honestly-scoped remaining problem (63 real locations, not 141+ scripts)."
  },
  {
    title: "Text digraph table: ~15 more repeating low-byte codes uncross-checked",
    area: "Text decoding",
    description: "A full-ROM scan found many more repeating low-byte values (0x25, 0x28, 0x2B, 0x33, 0x44, 0x46, 0x4A, 0x53, 0x64, 0x69, 0x6A, 0x87, 0xA9, ...) that are almost certainly more digraphs, several contextually guessable, but none cross-checked against a second independent occurrence yet -- this project's own bar for calling a byte VERIFIED."
  },
  {
    title: "Control byte 0xF6: WHAT is now well-evidenced, HOW (the real CPU mechanism) still open",
    area: "Text decoding",
    description: "2026-08-15: the original finding was a single live-VRAM observation (\"LP [F6] MP [F6]\" matching the live HUD's \"LP <n> MP <n>\"), never independently cross-checked. Found a real STATIC ROM string this pass (file 0xBE10) that decodes cleanly through TextDecoder to \"LP   [F6]   MP   [F6]Naechster Level\" -- a real German status-menu screen (Kraft/Reife/Wille stat labels precede it, \"Naechster Level\"/Next Level follows) -- independent confirmation from real ROM text data, not a live screen read. A second, unrelated real occurrence also found in the name-entry keyboard's own static layout string, right before the digit row. Cross-checked against rom_profiles.lua's own font.extraGlyphs formula: the predicted real font tile (file 0x22F60) decodes to a plain diagonal line, not a punctuation glyph -- ruling out \"unassigned printable character\" as an alternative reading. Still genuinely open: which real CPU code reads this byte and performs the substitution -- no live trace attempted yet."
  },
  {
    title: "scriptPointerTable's 3 special-cased index values (0x0B / 4 / 8)",
    area: "Script interpreter",
    description: "These 3 small index values redirect to fixed WRAM addresses instead of the normal ROM table lookup (some scripts are WRAM-resident, not ROM data) -- the mechanism is confirmed but their real-world meaning is not."
  },
  {
    title: "Bank 7's 'Templated' room format: door-data bytes remain semantically undecoded",
    area: "Room system",
    description: "RESOLVED for tile CONTENT and COLLISION 2026-08-14 (\"weiter bohren bis es fertig ist\" / \"ok weiter mit tür und kollision\"): the base-room RLE template + per-record (value,position) diff-overlay scheme is cracked and shipped (MapTable.applyTemplatedDiff, VERIFIED against all 64 real records -- 566/566 valid diff positions, tile_entropy 1.30-1.40 bits, zero outliers), and real per-metatile collision now dispatches to bank 7 the same way as bank 5/6 (live-verified via real love . screenshots). Still genuinely open: each record's own 4-byte prefix shows a remarkably clean, quantified 8-value structure across all 256 real bytes (bits0-1 always 0/1/2, matching the external doc's claimed 'open/closed/wall' 3-state layout; bits2-7 always 0-3) -- a strong structural lead, but with no live bank-7 gameplay to confirm which byte is which direction or what each state concretely means, it stays undecoded rather than guessed. The separate map-level 24-byte block does NOT share this clean pattern (wider value ranges on both fields) -- genuinely different data, not simply repeated."
  },
  {
    title: "The $413C cut-sequence table: only 2 of 30 steps decoded",
    area: "Room system",
    description: "Step 3 (room load) and step 5 (position set) are understood; the other 28 steps of this shared cut-transition state machine are not."
  },
  {
    title: "Enemy DEF formula: two candidate WRAM fields, neither confirmed live",
    area: "Combat",
    description: "The enemy species table's ATK field is VERIFIED via a live cross-check; DEF has two real candidate byte fields but neither has been confirmed against an actual live combat trace yet. Two specific leads are now ruled out (2026-08-12, and 2026-08-14's own bank-4 $4466 entity-command dispatcher trace, which turned out to be a real positioning/movement command family, not stat-reading) without a replacement found. HP's own real spawn-time roll formula (rollHP in CombatFormulas.lua) WAS found and implemented this same pass, but deliberately left unwired pending the real n=0 edge case the ROM itself branches on differently."
  },
  {
    title: "$1ED7 / $1F35 dispatcher families: remaining unmapped selectors",
    area: "Script interpreter",
    description: "UPDATED 2026-08-14: the $1F35 family's own read side is now traced end to end -- selector 0x0E ($4B4F) periodically scans the $C5A0 known-list and, via a per-entry helper ($4B19) and a real per-record tick handler ($404A), consumes what selector 0x0A/$4B70 writes (countdown/reload timers, a group-based branch, a further gated helper $4107). Selector 0x07 (previously unclassified) is now known to be its own separate small dispatcher. Still open: the exact real-world MEANING of the 8 action-code values themselves (0x04/0x05/0x0E/0x0F/0x1C/0x1D/0x1E/0x1F) -- two independent live-trace attempts at a real boss-defeat quest-flag moment both came back negative -- plus selectors 0x0B/0x0C/0x0D/0x0F's own deeper helper leaves ($0611/$0695/$08D4/$05EF/$3DCB/$24A7) and $1ED7's own selector range, neither re-visited this pass."
  },
  {
    title: "EntityStructLayout ($C200): 2 new fields found, most still unnamed by real-world meaning",
    area: "Combat / entity system",
    description: "2026-08-14: the whole $0C41-$0D1B getter/setter accessor block for this struct was found and mapped (see the Entity-Struktur page), revealing 2 fields beyond the previously-known 0-8 range (offsets +10/+11) with real getters/setters and real callers, plus evidence PARAM6/PARAM7 (+6/+7) are used as ONE paired 16-bit value by at least one real caller, not two independent bytes. PARAM2 (+2) is confirmed as the single most heavily-used accessor found anywhere in this struct (24+17 real callers, every ROM bank) and bridges directly to the $C4E0 actor-command array's own slot-scan code. A live-tested hypothesis that TYPE (+1) held a dynamic, attack-related per-frame state was RETRACTED the same day (mGBA watchpoints + a direct 600,000-step execution check both came back negative -- the 3 real static call sites exist, but the code containing them is never reached during a normal attack). Open: the real-world meaning of +10, +11, PARAM2, and TYPE's own real 3 call sites, if not attack-related."
  },
  {
    title: "Second boss encounter: evidence-based room placement, not an independently ROM-confirmed trigger",
    area: "Combat / room system",
    description: "2026-08-15: a full static census of all 1357 real message-settings records found exactly 5 sharing BOTH the first boss's own real species byte (0x16) AND its wider record structure -- a real, non-coincidental family. A full shadow-run of all 1357 real scripts found exactly 3 real scripts that actually trigger one of those sibling records via opcode 0xFE. WHICH (if any) of those 3 scripts ties to a specific room, and what triggers each of them (most likely an undecoded NPC/actor-slot script-index field), was not found despite substantial further digging -- see docs/reverse-engineering/events.md's 'Second boss investigation' section. The room this project places the encounter in went through 3 direct live corrections (fourthRoom -> fifthRoom -> sixthRoom) before settling on the room reached by walking west out of fourthRoom's own corridor -- a real, repeatedly-confirmed spatial fact, even though that room's own real tileset does not literally match the user's own 'looks like startRoom' description. Recorded honestly as an evidence-based implementation choice throughout, not a decoded ROM fact."
  },
  {
    title: "IN PROGRESS 2026-08-15: opcode 0x04's real $38F6 control-code table decoded AND wired, and the 0xBC/0xBD/0xBE palette-fade family is now wired too -- the interpreter's cursor tracks the real ROM further than ever, but a NEW real gap (0xF3's own unmodeled $1ED7 selector-0x10 side effect) still stands between it and the real literal text",
    area: "Script interpreter / Text decoding",
    description: "2026-08-15, full investigation arc: VictorySequence.lua's MYSTICQUEST_SCRIPT_INTERPRETER=1 integration was rewritten around BossSequenceInterpreter (real bank fix, real per-frame ticking, real 5-frame pacing -- shipped, tests green throughout). Live-testing kept converging on the same wrong cursor, 0x4798, stuck on opcode 0x00 -- a real desync unrelated to pacing. Root-caused: opcode 0x04's real handler ($333D) is a per-byte TEXT/CONTROL-CODE CLASSIFIER dispatching via a real jump table at $38F6, NOT a no-operand tick -- byte 0x00 there means 'text run done, re-fetch next real opcode', a different meaning than opcode 0x00's own top-level QUEUE_GATE. Disassembled all 16 real $38F6 slots (tools/rom/disasm.py): this is the exact same real 'multi-line textbox driver' this project's own much earlier '0xFF sub-table' investigation (see events.md, several sessions ago) already fully disassembled, just never connected to opcode 0x04. Bytes 0x14/0x15 are the real NAME-INSERTION mechanism (two distinct real WRAM string-pointer slots, $D79D/$D7A2), though the real string source's link to NameEntry.lua's own save format is still untraced. Control byte 0x11's real $36D0 'consume one more byte, re-enter classifier' bridge (conditional on real WRAM $D853 bit 7) was closed via a live mGBA write-watchpoint trace. Result: the interpreter's cursor tracked the real ROM byte-for-byte to bank=14 cursor=0x61d8, dispatching real opcode 0xC0 (HEAL_LP), then honestly stopped on the real, then-undecoded opcode 0xBD. SAME DAY, FURTHER CONTINUATION: fresh disassembly of 0xBD/0xBC/0xBE's shared leaf $1142 found a small, fully real, deterministic 6x11=66-tick pacing gate -- REVERSING the prior 'genuinely known-hard' assessment. All 3 opcodes wired (StandardScriptHandlers.paletteFadeCycle) -- the interpreter now dispatches 0xBD all 66 real times and continues into real opcode 0xF3 (PEEK_TWO_BYTE_GATE, already wired), genuine further progress. BUT a NEW, real, precisely-identified gap surfaces immediately after: 0xF3's own real handler ($11CE) unconditionally calls a real $1ED7 selector-0x10 dispatch on every invocation, gated or not, BEFORE its own release check -- a real side effect this project's existing .peekTwoByteGate implementation does not model. Without it, the two peeked bytes get misdispatched as top-level opcodes, diverging onto an already-wired-but-unrelated opcode chain that converges back on the SAME historical 0x4798 landing spot via a DIFFERENT root cause than the original mystery. 446/446 tests pass (a regression test locks in this exact new trajectory: 0xBD dispatched exactly 66 times, 0xF3 exactly once, final cursor 0x4798, runtime never stopped on a genuinely undecoded opcode). SAME DAY, FURTHER CONTINUATION: $1ED7 selector 0x10 disassembled and wired (StandardScriptHandlers.paletteFadeCompletionGate) -- a real 6-phase state machine, phases 1/3 gated on the already-modeled $C8E0/$CEE8 dual gate. Opcode 0xF3 now genuinely paces for 6 real ticks (verified via opcodeCounts). BUT the final cursor is still 0x4798 -- traced the exact divergence to 4 single-byte 'opcodes' dispatching cleanly right after 0xF3 releases, strongly suggesting one of the state machine's own 2 untraced sub-calls (phase 2's $26DC/$04A4, phase 4's $0375/$44A5/$28C2/$289B/$28AA/$2EF7 -- several routines deep, some already partially known from other investigations) ALSO advances the real script cursor internally, unmodeled by this pass. Three layers deep now, genuinely open-ended. 451/451 tests pass, zero regressions. Remaining, now much more precisely scoped next steps: trace phase 2/4's own sub-calls for a real internal $3727 fetch, trace the real 0x14/0x15 string source to NameEntry.lua's save format, and bridge this interpreter into the visible phase machine -- none complete yet. UPDATE 2026-08-16 (task #160 follow-up): the $C8E0/$CEE8 dual gate this entry's own phases 1/3 already wait on now has a concrete real-world meaning, found via task #160's independent access-analysis investigation -- $C8E0 is the real queue depth of a fully-disassembled ROM->VRAM tile-streaming DMA system (bank 0, $2D57-$2E31); phases 1/3 are genuinely waiting for pending graphics transfers to finish, not an opaque flag. Doesn't close the remaining phase 2/4 sub-call trace above, but replaces guesswork about WHAT is being waited for with a real, documented answer -- see rom-map.md's own 'the real graphics-loading mechanism' section for the full disassembly."
  },
  {
    title: "Real hardware OAM-vs-WRAM sprite offset: real finding, general fix tried and REVERTED after a live regression",
    area: "Rendering / entity system",
    description: "2026-08-15: a direct dump of the real hardware OAM table shows the player sprite's WRAM position ($C244/$C245) copies into OAM completely unshifted, but real Game Boy hardware always encodes OAM sprite position as (true top-left)+(8,16) -- fixed PPU silicon behavior. So the true visible position is 8px left/16px up from what this project has always rendered. A general fix was implemented (Player.lua's RENDER_OFFSET_X/Y + renderPosition(), applied at every player draw call in Field.lua/VictorySequence.lua/BattleIntro.lua) and looked fine on a couple of screenshot spot-checks. Direct user report right after actually launching and playing the app: the FIRST boss room (startRoom, Field.lua) now looked visibly shifted -- a real regression. Root cause: startRoom's own sprite/collision data was historically calibrated the OLD, unshifted way, and applying the real hardware offset on top of that already-self-consistent (if differently-conventioned) calibration broke it. FULLY REVERTED: every player draw call back to the raw self.player.x/y, matching this project's original convention everywhere; fourthRoom's own landing back to the raw ROM value (120,112) too. The underlying hardware fact is still real and stays documented in Player.lua (RENDER_OFFSET_X/Y, renderPosition() -- unused by any draw call), but safely applying it would need re-verifying every room's own historical calibration individually first, a real, much larger task than this session covered. Lesson recorded plainly: a couple of screenshot spot-checks are not the same rigor as actually playing the app."
  },
  {
    title: "Room connectivity + player spawn position: BOTH CLOSED 2026-08-16 for wipe-style cut transitions -- but WHICH story moment triggers most of them stays open",
    area: "Room system",
    description: "Originally (2026-08-13): 6+ independent static-analysis passes found room connectivity was genuinely script/bytecode-driven (no lookup table) and no ROM code at all could be found writing the player's own landing WRAM ($C244/$C245) -- 'zero hits for every literal-address-load pattern tried'. CLOSED 2026-08-16 (direct instruction 'then do those blockers. dont stop before they are solved. if one strategy does not work try another one'): a live hardware WATCHPOINT on $C244/$C245 (not a static byte search -- the real write turned out to be INDIRECT, LD (HL),D/LD (HL),E, exactly the blind spot a literal-address grep can never cover) found the real write site and its full live call chain back 6 hops to the script cursor itself, landing on opcode 0xF4/$11B7 (already known, see 'PEEK_TWO_BYTE_GATE' in the opcode table -- a 2026-08-14 session had already found this mechanism for exactly 2 transitions by hand). Static pattern search then found the real, GENERAL record shape these bytes live inside: a 186-record table in bank 14 (00 05 F4 roomSelector subIndexByte tileCol tileRow 00 0B), zero false positives anywhere else in the whole ROM, cross-validated byte-exact against both known transitions. A direct follow-up live trace of the real $4395 (CALL $026DC) call site found the SAME record's own first operand byte IS the target roomSelector, fed unmodified -- closing room connectivity too, not just landing position (statistically confirmed: this field ranges exactly 1-15 across all 186 records, matching roomSelectorTable's own real 16-entry size precisely). Real bonus: resolves fourthRoom's own long-standing 'roomSelector 0 or 1?' ambiguity (confirmed 1). See the dedicated 'Raum-Übergänge' tab for the full, browsable table. STILL OPEN: the 186 raw records collapse to 82 genuinely distinct real transitions, but only 2 are currently live-verified AND wired into actual gameplay -- 36 of the other 80 target the unknownRoomA family (real, ROM-verified proof it's genuine content, not dead data, first ever found for this long-mysterious room family), 9 more target roomSelector 14 (a brand-new, second open mystery matching no currently-known room). A time-boxed live search (every wall of every currently-reachable room, correctly-scaled hold windows, plus Amanda's full dialogue) found zero new triggers -- these likely require story/quest progression past this project's own current checkpoint chain, or a room not yet wired as walkable at all. Not pursued further -- a real, substantial, well-scoped follow-up for whoever continues. MAJOR UPDATE 2026-08-19/20 (a full session): the general redirect MECHANISM that eventually reaches bank 14 is now fully reverse-engineered and twice live-confirmed -- see the 'Cross-actor dispatch mechanism' entry above for the full trace. A DECISIVE, exhaustive live boundary flood-fill of every currently-reachable room (startRoom/fourthRoom/fifthRoom/willyRoom/secondRoom/thirdRoom, fresh emulator state per probe, zero cross-contamination) found ZERO hidden passages anywhere -- these 6 rooms are genuinely, fully enclosed by ordinary walking. A follow-up, more targeted live check found 2 high-occurrence, unwired CutTransitionTable landings reused across the willyRoom family ((80,72), open floor; (136,112), wall-adjacent) and tested them precisely (axis-exact walk-to + an A-button press, 6 of 8 real room/target combinations landing exactly on target): a clean, decisive negative -- neither triggers anything. An attempted door-chain into unknownRoomA (2026-08-19) was built, briefly shipped, then RETRACTED the same day after its own floor data turned out broken (see the 'Collision' entry above for the full story and its eventual resolution). Honest, current bottom line: this project has now checked essentially every real cross-script-reference mechanism it has ever cracked (CHAIN, the $26DC/roomSelectorTable grid, the WRAM queue, literal constants, ordinary walking, direct interaction) for a natural trigger into any of the remaining 80 transitions, and found none -- the real trigger for unknownRoomA/roomSelector-14 content almost certainly requires either story/quest progression this project's own checkpoint chain doesn't reach yet, or a genuinely different, not-yet-cracked subsystem (an NPC/dialogue-trigger table, an item-use handler) this project hasn't found at all. UPDATE 2026-08-20: rather than keep pushing on this specific plateau, connected the already footprint-verified worldMapRoom_131/132 pair (previously dev-shortcut-only) to the real reachable graph via a new, honestly-labeled ENGINEERING-CHOICE door on seventhRoom's own real east edge -- not a new ROM-trigger finding, but a real increase in explorable content (11 rooms reachable via normal play, up from 9), live-confirmed in both directions via scripted love . runs. See the roadmap's own P0 world-scope row for the full trace. UPDATE 2026-08-20, same day, direct user directive ('die gesamte Konnektivität aller Räume entschlüsseln und in die App und der Website einbauen'): re-scoped honestly -- structural/edge-match connectivity is already exhaustively closed catalog-wide (6 real pairs, 1 walkable; bank 7 confirmed a flat catalog, 0.0% match), and this specific script-trigger plateau isn't reopened without a genuinely new angle, none found. Applied the same worldMapRoom_131 recipe to unknownRoomA_8..13 instead: its 2026-08-19 broken-floor-data retraction no longer applies (F8 already got real, position-aware collision for this family) -- re-checked adjacency properly this time (against the room family's own dedicated metatile table, not the generic one the original scan used) and found a decisive negative, so wired all 6 as independent spokes off worldMapRoom_132, zero adjacency claim, honestly ENGINEERING CHOICE. 3 real bugs caught by simulating the exact production movement code before any live run (a test-code coordinate mix-up, a real zone collision with the pre-existing exit, and Player:update's own unconditional bounds clamp silently relocating a pre-existing landing spot onto a naive first version of one new zone). Live-confirmed round-trip for 3 of 6 rooms (the roomiest, a mid-constrained case, and the most constrained); the other 3 rely on the same proven BFS evidence, not individually live-GUI-tested. 17 rooms now reachable via normal play (up from 11). eighthRoom/ninthRoom (already linked to each other) investigated as the next candidate, found genuinely fragile, deliberately deferred rather than rushed. Bank 7's 64 rooms and the ~300 other catalog records have zero structural evidence and would each need this same per-room rigor -- no bulk shortcut exists without fabricating connections."
  },
  {
    title: "NPC spawn MECHANISM traced end-to-end (2026-08-16) -- but it's a RUNTIME-INDEXED shared table, not a static per-room placement table",
    area: "NPCs / entity spawning",
    description: "This project's own long-standing 'PRNG-placed, transform undecoded' note on secondRoom's two NPCs finally has a concrete, live-traced mechanism behind it. The same watchpoint + frame-snapshot-diff technique that closed room connectivity found the real chain: a proximity/distance check (bank 3, ~$44A0, |D-H|/|E-L| vs threshold 4) calls the ALREADY-KNOWN combat PRNG ($2B1E, see docs/reverse-engineering/combat.md) to get an index into a real 24-byte-stride table (bank 3, CPU $5f5a), whose record embeds a pointer to a SECOND real 24-byte record of small, tile-ID-shaped bytes, before calling the already-known $0A74 entity allocator. The two live-captured spawn events used table indices 99 and 121 -- their sprite sub-records differ by EXACTLY +0x20 on every varying byte, a byte-exact match (via a totally independent method, live OAM tile-ID capture) to this project's own already-confirmed 'characterB's OAM tile IDs are characterA's own +0x20' fact. Decisive corroboration, but HONESTLY NOT a static placement table: the index is computed AT RUNTIME (RNG-influenced), which is exactly why static search alone never found one. The full 24-byte record semantics (both record types), the exact RNG-roll -> index derivation, and the table's real total extent (sampled ~175 entries, likely larger) remain undecoded. See src/import/ActorDefinitionTable.lua."
  },
  {
    title: "Magic/spell system: which record is which spell + its real MP cost -- CLOSED (2026-08-19); what casting one actually DOES in combat still open",
    area: "Menu / Magic",
    description: "2026-08-18's own price-field pass had already closed item shop pricing but explicitly left 'the real castable-spell table... genuinely unfound', with 'search for any handler that reads or writes $D7B6/$D7B8 (curMP/maxMP) directly' as the concrete next angle. Direct static ROM-wide scan for every literal reference to those 4 WRAM bytes found real hits, including bank 2 CPU $718F-$71AB (menu-context) and its battle-context sibling $6660-$667E -- both fully disassembled: LD HL,0x5DEC / CALL 0x768C resolves a real, 1-based, 16-byte-stride table index (0x768C itself disassembled too: (base+1)+((A AND 0x7F)-1)*16), then LD A,(HL) / AND 0x1F reads the real MP-cost byte, SUB against curMP with a carry-gated insufficient-funds bail before committing the new curMP. Decisive discovery: the resolved table base ($5DED, file 0x9DED in bank 2) is exactly ItemTable's own fileOffset (0x9DE5) + 8 -- this is NOT a separate table, it's byte 8 (categoryByte) of the SAME already-decoded 16-byte item record, masked to its low 5 bits. Cross-checked against the same external walkthrough the price-field pass used: records 0-7's own categoryByte AND 0x1F reads 2,1,1,1,1,2,2,3 -- an exact, in-order, 8/8 match to Cure/Heal/Sleep/Mute/Fire/Ice/Lightning/Nuke's real MP costs, corroborated by the records' own already-decoded German name fragments (Lebe/Salb/Blok/Ruhe/Flam/Eis/Bliz/Bomb). This RETRACTS this project's own earlier assumption that records 0-7 were 'found/thrown combat items, never sold' (based only on price=0, which is equally consistent with 'these are spells, not gold-purchased') -- the website's items table previously mislabeled this exactly backwards (isSpell flagged 8+ instead of 0-7), now corrected. Honest remaining scope: this closes WHICH record is which spell and its real MP cost/deduction code -- it does NOT close what casting a given spell actually does in combat (elemental damage/heal-amount/status-effect formulas), and none of this is wired into live gameplay yet -- Menu.lua's 'Magie' option still honestly closes immediately, matching the real ROM's own live-confirmed behavior for a fresh character with zero known spells (no real trigger for LEARNING a spell has been found, live-verified via a direct mGBA menu probe this same pass)."
  },
  {
    title: "Magic/spell EFFECTS: real dispatch architecture found (2026-08-19, same day) -- 2 of the effect categories understood, elemental/attack spells still open",
    area: "Menu / Magic",
    description: "Direct same-day follow-up ('Magic/Spell: Effekt-Formeln suchen'). Traced the real callers of the MP-deduction routine and disassembled what happens after a successful cast. Real entry point: bank 2 CPU $6FBE (sets bit 6 of WRAM $D86F, a real 'spell cast in progress' flag; reads the selected spell index from $D88B). After MP deduction succeeds, the real ROM walks a genuine PER-EFFECT-CATEGORY dispatch chain -- each category has its own small membership table, tested via a shared resolver (CALL $7155, carry-clear = 'not this category, try the next table'): table $7B29 is the LP-restore category -- spell index 1 (Cure) computes its heal amount via the already-known combat PRNG ($2B1E) feeding a level-indexed growth-curve lookup ($2B7B/$2B8B) -- the SAME two real routines already found driving the character level-up HP/MP growth formula, real concrete evidence the two systems share underlying table infrastructure; table $7B31 is a STATUS-CURE category, not a second HP-heal -- spell index 2 (Heal) sets a 4-bit mask and conditionally calls 4 distinct per-bit cure routines, resolving this project's own Cure-vs-Heal naming ambiguity (Cure restores LP, Heal cures status ailments -- the opposite of the literal English words, but matching German 'Salbe'=balm/ointment); table $7B38 is a third category gated on WRAM $D87E==0xFF, involving a 4-byte per-character block copy, not fully traced. Honest remaining scope: the elemental/attack spells' own effect category (Fire/Ice/Lightning/Nuke, indices 5-8) was not located this pass -- very plausibly a further table beyond $7B38 in the same chain -- and the growth-curve table's own exact numeric content stays undecoded. See src/import/ItemTable.lua's own matching doc comment for the full disassembly."
  },
  {
    title: "Magic/spell EFFECTS: the FULL 8-category dispatch chain mapped (2026-08-19, same day) -- items and spells share one effect system; Ice's own effect genuinely missing",
    area: "Menu / Magic",
    description: "Direct continuation of the entry above ('Element-/Angriffszauber-Kategorie suchen') -- the chain does not end at $7B38. Full real order (each table's own JR NC falls through to the next; the last uses RET NC): $7B29 -> $7B31 -> $7B38 -> $7B46 -> $7B3D -> $7B40 -> $7B43 -> $7B48. Each table is a real, null-terminated list of 1-based item/spell indices -- read every table's raw bytes directly from ROM and cross-referenced against ItemTable's own decoded names: $7B29={Lebe(spell),Lebe,S-Lebe,Magi,S-Magi,Elixier,Bonbon} (LP-restore); $7B31={Salb(spell),Salbe,Auge,Bewege,Spruch,Allheil} (status-cure); $7B38={Rubin,Smaragd,Saphir,Diamant} (gems, still-unidentified $D87E-gated mechanic); $7B46={Kristal}; $7B3D={Ruhe(spell),Schlaf}; $7B40={Blok(spell),Stille}; $7B43={2 still-undecoded-name records}; $7B48={Flam(spell),Bliz(spell),Bomb(spell),Flamme,Lava} (the elemental/attack-adjacent catch-all). Decisive DESIGN finding: this is ONE shared effect system serving BOTH the Magic-menu spells (MP-gated) AND the Dinge-menu items (which structurally never reach the MP gate, since its own check is index<9) -- the Cure spell and the shop potions literally run the same code, extending this project's own older, tentative 'clean single-bit progression, plausibly which status this cures' note (2026-08-16) into decisive, code-level confirmation. Self-caught correction: an earlier same-day note assigned Sleep/Mute purely from the external walkthrough's listed order (both cost 1 MP, indistinguishable by cost) -- this table evidence resolves it the OTHER way: Blok groups with shop item 'Stille' (Silence/Mute) -> Blok=Mute; Ruhe groups with 'Schlaf' (Sleep) -> Ruhe=Sleep. Honest, explicitly flagged anomaly, not forced to a guess: spell index 6 (Ice) does not appear in ANY of these 8 tables -- either a 9th table exists reached some other way not yet traced, or Ice's real effect lives entirely outside this chain. Not resolved this pass. See src/import/ItemTable.lua's own matching doc comment for the full disassembly."
  },
  {
    title: "Magic/spell EFFECTS: the Ice anomaly REFRAMED -- the whole 8-category chain is FIELD/MENU-only, battle-cast spells go elsewhere (2026-08-19, same day)",
    area: "Menu / Magic",
    description: "Direct same-day follow-up ('Magic/Spell: Restdetails klären'). Traced the whole $7B29-$7B48 chain's own single real caller (found via the same literal-address-bytes search used throughout this investigation): a real jump table at bank 2 CPU $404E, structurally a menu/field action-state dispatch. Then disassembled the BATTLE-context MP-deduction sibling's ($6660-$667E) own real continuation, CALL $71AC (fires on both the success AND the insufficient-MP failure path) -- and found it does NOT lead into the same chain. $71AC copies the battle-context selected-index cell ($D6EF) into the SAME $D88B cell the menu path reads (real, decisive evidence both contexts share one 'currently selected' cell) -- but then CP 0x09 / RET C returns IMMEDIATELY for any real spell index (1-8), before reaching anything resembling $7B29. Only item indices (9+) continue further there, into real ammo/charge-count logic ($D6F0/$D6F1/$D6C5), not traced in depth. Honest, evidence-grounded reframe: the $7B29-$7B48 chain is the FIELD/MENU-context spell-and-item-use effect system (usable from the 'Magie'/'Dinge' menu outside combat) -- NOT something battle-cast spells go through at all. This reframes the Ice anomaly from 'missing/broken' to 'this spell's FIELD effect may genuinely not exist, while its real BATTLE damage formula -- along with Fire's/Lightning's/Nuke's own real battle damage, since their presence in $7B48 may only be their field-use effect, not their combat one -- lives in a genuinely separate, not-yet-located code path'. Bonus: $7B38's/$7B43's own completion routines ($2F9E/$2FD4) turned out to be a real, general 16-slot inventory allocator at WRAM $D7E1 -- confirming gems/Kristal are a treasure/key-item pickup category, structurally unrelated to combat magic despite sharing the same dispatch mechanism. See src/import/ItemTable.lua's own matching doc comment for the full disassembly."
  },
  {
    title: "Magic/spell findings wired into the interpreted app (2026-08-19, same day) -- a second, live-affecting backwards-split bug found and fixed, Magie now a real, browsable sub-menu",
    area: "Menu / Magic",
    description: "Direct consolidation pass ('konsolidiere alles... baue es in die app... ein'). Found a SECOND, real, live-gameplay-affecting instance of the exact backwards item/spell split bug already fixed on this website earlier the same day: Inventory.lua's own Inventory.new split ItemTable records using 'index >= boundary -> spellCatalog', exactly backwards (the real spells filed as items, the real shop items filed as spells) -- the interpreted app's own copy of the website's isSpell bug, never propagated back. Fixed the direction, updated Inventory.lua's own tests. While fixing it, found a SECOND real gap: Menu.lua's option-select logic had a branch for Dinge (opens once items exist) and Waffe (equips), but NONE for Magie at all -- it always fell through to the generic no-op close, even once the split fix made inventory.spells real, non-empty data. Added the missing branch: Magie now opens a real sub-list once granted spells exist, showing each spell's real name plus its real mpCost (e.g. 'Salb 1MP'). Selecting a spell is a deliberate no-op (no MP deduction, no combat/field effect -- that ROM code is found but not independently gameplay-verified yet, and spells are KNOWN not consumed, unlike items). Live-verified via 3 real love. screenshots: the granted Dinge list correctly shows the real shop item 'Lebe' (record 8); the granted Magie list correctly shows the real spell 'Salb 1MP' (record 1, Heal) instead of the old, wrong 'S-Lebe' shop item; a FRESH, ungranted character's real, live-tested behavior (Magie closes immediately) is completely unaffected. luajit tests/run_tests.lua: 580/580 pass."
  },
  {
    title: "Puppeteering driver: first successful live injection in this project's history -- opcode 0xBA reached and confirmed for real (2026-08-19)",
    area: "Script interpreter",
    description: "Five real rounds deep into a long-parked investigation (task #150's own 2026-08-15/16 attempts, and this project's own roadmap, had characterized a working live-injection ('puppeteering') driver as needing 'the same scale of effort as a full investigation session', never actually built). Round 1: found the earlier technique's own bank-switch primitive (poking mGBA's cached currentBank field) has ZERO real effect, proven by direct test -- the correct primitive is a real $2100 bus write. Round 2: fully disassembled $29FB/$2A0A (the real bank-stack push/pop) for the first time and replicated the push protocol. Round 3: empirically disproved a 'filter by bank-stack depth' hypothesis (3,000,000-step real scan: depth is constant across all natural $3727 hits; all 11 distinct callers share the identical CALL $3727/RET shape). Round 4: studied a clean, non-injected reference trace and found the REAL remaining bug -- the 'disciplined push' added an extra stack level, shifting the real subsequent pop UP by one. Round 5 (this pass): fixed it (overwrite the existing top slot in place, don't push a new frame) and started the injection from an ALREADY-ACTIVE real script context instead of an arbitrary early-boot moment -- live execution REACHED THE REAL 0xBA HANDLER ($0EB2) FOR THE FIRST TIME, and ran it: confirmed byte-for-byte against ScriptOpcodeTable.lua's own existing static analysis (a real CALL $0A74 entity-slot allocation, a genuine 20-slot live scan, populating a real slot with real data). The methodology is now general and reusable -- a working live-injection primitive that can be pointed at the other 5 known-hard opcodes or any other unreached script (including the unknownRoomA family). Honest remaining scope: full entity-struct field semantics and independent confirmation the despawn half completes cleanly aren't closed; not wired into the Lua interpreter yet. See src/import/ScriptOpcodeTable.lua's own 0xBA doc comment and docs/reverse-engineering/events.md's dated 2026-08-19 entries (5 rounds) for the complete trace."
  },
  {
    title: "Player-attacks-enemy damage: the already-decoded real PRNG formula is now WIRED into gameplay (2026-08-19)",
    area: "Combat",
    description: "The player-attacks-enemy direction's real formula (same PRNG shape as $50AC's already-wired enemy-attacks-player one -- see the 2026-08-16 'MAJOR CORRECTION' finding elsewhere on this project) had been fully decoded but never actually applied: Enemy:hit() still dealt a flat PLAYER_ATTACK_DAMAGE=4 constant directly, unlike contact damage (already routed through CombatFormulas.rollDamage). Added CombatFormulas.rollPlayerAttackDamage(base, noiseByte) -- a genuinely separate function, not a call to rollDamage with a fabricated ATK/DEF pair, since this direction's own real ROM chain has no enemy-side DEF term at all (3 separate static-analysis passes looked for one and found none). Enemy:hit(noiseByte) now calls it; Field.lua's attack-hit code draws the noiseByte from the SAME shared combatNoise PRNG stream contact damage already uses, matching the real ROM's own single $C0B0/$C0B1 counter pair (not a separate stream per direction). Changes nothing observable in the currently-playable game -- base=4's own noise contribution mathematically floors to 0 for every possible byte, the exact same floor-rounding coincidence already documented for the enemy-attacks-player base=3 case, now also test-covered -- but the real, authentic formula is now genuinely running underneath, and will produce real, visible damage variance automatically the moment a second weapon or enemy pushes the base value past 256 (the real threshold where the noise term becomes visible). 3 new tests added, luajit tests/run_tests.lua: 587/587 passing."
  },
];
